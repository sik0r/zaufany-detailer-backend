{% extends 'workshop_panel/base.html.twig' %}

{% block header %}Dashboard{% endblock %}

{% block body %}
<div class="space-y-6">
    <div class="card bg-base-100 shadow-xl">
        <div class="card-body">
            <h2 class="card-title">
                Witaj w panelu zarządzania warsztatem!
            </h2>
            <p>
                Witaj {{ app.user.firstName }} {{ app.user.lastName }}. Jesteś zalogowany jako pracownik firmy <span class="font-semibold">{{ app.user.company.name }}</span>.
            </p>
        </div>
    </div>

    <div class="card bg-base-100 shadow-xl">
        <div class="card-body">
            <h3 class="text-xl font-semibold mb-4">Twoje Dane</h3>
            <dl class="divide-y divide-base-300">
                <div class="py-3 grid grid-cols-1 sm:grid-cols-3 gap-4">
                    <dt class="text-sm font-medium opacity-75">
                        Email
                    </dt>
                    <dd class="text-sm sm:col-span-2">
                        {{ app.user.email }}
                    </dd>
                </div>
                <div class="py-3 grid grid-cols-1 sm:grid-cols-3 gap-4">
                    <dt class="text-sm font-medium opacity-75">
                        Numer telefonu
                    </dt>
                    <dd class="text-sm sm:col-span-2">
                        {{ app.user.phoneNumber | default('Nie podano') }}
                    </dd>
                </div>
            </dl>
        </div>
    </div>

    <div class="card bg-base-100 shadow-xl">
        <div class="card-body">
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
                <h2 class="card-title">
                    Twoje warsztaty
                </h2>
                <button type="button" class="btn btn-primary">
                    Dodaj warsztat
                </button>
            </div>
            <p class="opacity-75 mt-2">
                Zarządzaj profilami swoich warsztatów.
            </p>
            <div class="mt-4">
                <p class="text-center opacity-60 py-4">
                    Na razie nie masz żadnych warsztatów. Skorzystaj z przycisku "Dodaj warsztat", aby utworzyć nowy profil.
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %} 